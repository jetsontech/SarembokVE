/* SAREMBOK_UI_RUNTIME_V6_20260915 */
(function(){
'use strict';
let ws=null,rpcId=0,pending=new Map(),sessionToken='',activeRequest=null,reconnectTimer=null,reconnectDelay=1000,connecting=null;
const $=id=>document.getElementById(id);
function setStatus(text,online){const s=$('runtime-status'),d=$('runtime-dot');if(s)s.textContent=text;if(d)d.className='dot '+(online?'online':'offline')}
function setMeta(text){const e=$('connection-meta');if(e)e.textContent=text}
function escapeHtml(s){return String(s??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]))}
function normalizeText(v){if(v==null)return '';if(typeof v==='string')return v;if(Array.isArray(v))return v.map(normalizeText).filter(Boolean).join('');if(typeof v==='object'){for(const k of ['response','text','content','delta','message','output','answer'])if(v[k]!=null)return normalizeText(v[k]);try{return JSON.stringify(v,null,2)}catch{return String(v)}}return String(v)}
function youtubeEmbed(url){try{const u=new URL(url);let id=u.searchParams.get('v');if(!id&&u.hostname.includes('youtu.be'))id=u.pathname.replace(/^\//,'');if(!id)return '';id=id.replace(/[^a-zA-Z0-9_-]/g,'');return '<div class="media-card"><div class="media-label">VIDEO</div><iframe class="media-frame" style="display:block;width:100%;min-height:420px;border:0;border-radius:10px;background:#000" src="https://www.youtube.com/embed/'+id+'" title="Sarembok video" loading="lazy" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" allowfullscreen></iframe><a class="media-link" href="https://www.youtube.com/watch?v='+id+'" target="_blank" rel="noopener">Open on YouTube ↗</a></div>'}catch{return ''}}
function directYoutube(raw){const m=String(raw??'').trim().match(/^(https?:\/\/(?:www\.)?(?:youtube\.com\/watch\?v=[^\s&]+|youtu\.be\/[^\s?]+)(?:[^\s]*)?)$/i);return m?youtubeEmbed(m[1]):''}
function renderDirectiveBlocks(raw){let text=String(raw??''),blocks=[];text=text.replace(/:::video\s*([^\n]*)\n([\s\S]*?)\n:::/gi,(_,title,body)=>{const lines=String(body).split(/\r?\n/).map(s=>s.trim()).filter(Boolean),url=lines.find(v=>/^https?:\/\//i.test(v))||'';const embed=youtubeEmbed(url);blocks.push(embed||('<div class="media-card"><div class="media-label">VIDEO</div><div class="media-title">'+escapeHtml(title||'Sarembok video')+'</div><a class="media-link" href="'+escapeHtml(url)+'" target="_blank" rel="noopener">Open media ↗</a></div>'));return '\n'});text=text.replace(/:::music\s*([^\n]*)\n([\s\S]*?)\n:::/gi,(_,title,body)=>{const lines=String(body).split(/\r?\n/).map(s=>s.trim()).filter(Boolean),url=lines.find(v=>/^https?:\/\//i.test(v))||'',embed=youtubeEmbed(url);blocks.push(embed?embed.replace('VIDEO','AUDIO').replace('Open on YouTube','Open media'):'<div class="media-card"><div class="media-label">AUDIO</div><div class="media-title">'+escapeHtml(title||'Sarembok audio')+'</div></div>');return '\n'});if(blocks.length)text+='\n\n'+blocks.join('\n\n');return text}
function renderMarkdown(raw){const withBlocks=renderDirectiveBlocks(raw),extracted=[];const placeholdered=withBlocks.replace(/<div class="media-card">[\s\S]*?<\/div>/g,m=>{const token='@@MEDIA_'+extracted.length+'@@';extracted.push(m);return token});let text=placeholdered.replace(/\\([*_`#>|])/g,'$1');let html=escapeHtml(text);html=html.replace(/(https?:\/\/[^\s<]+)/g,'<a href="$1" target="_blank" rel="noopener">$1</a>');html=html.replace(/```(?:[a-zA-Z0-9_-]+)?\n?([\s\S]*?)```/g,(_,code)=>'<pre><code>'+escapeHtml(code.trim())+'</code></pre>');html=html.replace(/`([^`\n]+)`/g,'<code>$1</code>');html=html.replace(/\*\*([^*\n]+)\*\*/g,'<strong>$1</strong>');html=html.replace(/__([^_\n]+)__/g,'<strong>$1</strong>');html=html.replace(/^###\s+(.+)$/gm,'<h4>$1</h4>');html=html.replace(/^##\s+(.+)$/gm,'<h3>$1</h3>');html=html.replace(/^#\s+(.+)$/gm,'<h2>$1</h2>');html=html.replace(/^[-*]\s+(.+)$/gm,'<li>$1</li>');html=html.replace(/(<li>.*?<\/li>)(?:\s*<br>)+(?!<li>)/gs,'<ul>$1</ul>');html=html.replace(/^>\s+(.+)$/gm,'<blockquote>$1</blockquote>');html=html.replace(/\n{2,}/g,'</p><p>');html=html.replace(/\n/g,'<br>');html='<p>'+html+'</p>';extracted.forEach((fragment,i)=>{html=html.replace(new RegExp('@@MEDIA_'+i+'@@','g'),fragment)});return html}
function renderStructured(data){const sr=data&&data.structuredResponse;if(!sr||typeof sr!=='object')return '';const parts=[];if(sr.title)parts.push('<h3>'+escapeHtml(sr.title)+'</h3>');for(const section of Array.isArray(sr.sections)?sr.sections:[]){if(!section||typeof section!=='object')continue;if(section.heading)parts.push('<h4>'+escapeHtml(section.heading)+'</h4>');const body=section.content??section.text;if(body)parts.push(renderMarkdown(normalizeText(body)));const items=Array.isArray(section.items)?section.items:[];if(items.length)parts.push('<ul>'+items.map(i=>'<li>'+renderMarkdown(normalizeText(i)).replace(/^<p>|<\/p>$/g,'')+'</li>').join('')+'</ul>')}return parts.join('')}
function renderInto(bubble,text,data){if(!bubble)return;bubble.innerHTML=renderStructured(data)||renderMarkdown(text);const c=$('conversation');if(c)c.scrollTop=c.scrollHeight}
function addMessage(role,text,data){const empty=$('empty-state');if(empty)empty.remove();const wrap=document.createElement('div');wrap.className='message '+role;const label=role==='user'?'YOU':'SAREMBOK';wrap.innerHTML='<div><div class="message-label">'+label+'</div><div class="bubble" tabindex="0"></div></div>';const c=$('conversation');if(c){c.appendChild(wrap);c.scrollTop=c.scrollHeight}const bubble=wrap.querySelector('.bubble');renderInto(bubble,text,data);return bubble}
function showWorking(show){const t=$('typing'),s=$('send-btn'),x=$('stop-btn');if(t)t.hidden=!show;if(s)s.hidden=show;if(x)x.hidden=!show}
async function initSession(force=false){if(sessionToken&&!force)return sessionToken;const r=await fetch('/session',{cache:'no-store',credentials:'same-origin',headers:{Accept:'application/json'}});if(!r.ok)throw new Error('Secure session failed ('+r.status+')');const d=await r.json();sessionToken=String(d.sessionToken||'');if(!sessionToken)throw new Error('Secure session returned no token');return sessionToken}
function scheduleReconnect(){if(reconnectTimer||activeRequest)return;reconnectTimer=setTimeout(()=>{reconnectTimer=null;connect().catch(()=>{})},reconnectDelay);reconnectDelay=Math.min(reconnectDelay*2,8000)}
async function connect(){if(ws&&ws.readyState===WebSocket.OPEN)return ws;if(connecting)return connecting;connecting=(async()=>{setStatus('AUTHENTICATING',false);setMeta('ESTABLISHING SECURE SESSION');await initSession();await new Promise((resolve,reject)=>{const proto=location.protocol==='https:'?'wss:':'ws:',socket=new WebSocket(proto+'//'+location.host+'/ws');let settled=false;const timer=setTimeout(()=>{if(!settled){settled=true;try{socket.close()}catch{}reject(new Error('WebSocket connection timeout'))}},10000);socket.onopen=()=>{if(settled)return;settled=true;clearTimeout(timer);ws=socket;reconnectDelay=1000;setStatus('ONLINE',true);setMeta('SECURE RUNTIME SESSION');resolve();refreshRuntime()};socket.onerror=()=>{if(!settled){settled=true;clearTimeout(timer);reject(new Error('WebSocket connection failed'))}};socket.onclose=()=>{if(ws===socket)ws=null;setStatus('OFFLINE',false);setMeta('RECONNECTING…');for(const [id,q] of pending){q.reject(new Error('Connection closed'));pending.delete(id)}if(activeRequest&&!activeRequest.finished)finishMessage('Connection closed before Sarembok completed the request.',true);scheduleReconnect()};socket.onmessage=e=>handleMessage(e.data)});return ws})();try{return await connecting}finally{connecting=null}}
function handleMessage(raw){let data;try{data=JSON.parse(raw)}catch{return}const method=String(data.method||''),params=data.params||{};if(['SarembokChat.delta','SarembokChat.stream','chat.delta','stream.delta'].includes(method)){const id=params.id||params.requestId||data.id,delta=normalizeText(params.text??params.delta??params.content??params.response??data.result??'');const q=id&&pending.get(id);if(q&&q.onDelta&&delta)q.onDelta(delta);else if(activeRequest&&activeRequest.onDelta&&delta)activeRequest.onDelta(delta);return}if(data.id!=null){const q=pending.get(String(data.id))||pending.get(data.id);if(!q)return;pending.delete(String(data.id));pending.delete(data.id);if(data.error)q.reject(new Error(String(data.error.message||'RPC error')));else q.resolve(data.result??data)}}
async function sendRPC(method,params={},onDelta=null){await connect();if(!sessionToken)await initSession();const id='rpc-'+(++rpcId),payload={...params,sessionToken};return new Promise((resolve,reject)=>{const timer=setTimeout(()=>{pending.delete(id);reject(new Error('Request timed out: '+method))},125000);pending.set(id,{resolve:v=>{clearTimeout(timer);resolve(v)},reject:e=>{clearTimeout(timer);reject(e)},onDelta});try{ws.send(JSON.stringify({jsonrpc:'2.0',id,method,params:payload}))}catch(error){clearTimeout(timer);pending.delete(id);reject(error)}})}
function resultText(r){return r?normalizeText(typeof r==='string'?r:(r.response??r.text??r.content??r.output??r.answer??r.message??'')):''}
function finishMessage(text,error=false,data=null){if(!activeRequest)return;const request=activeRequest;request.finished=true;renderInto(request.bubble,text||'Directive completed.',data);if(error)request.bubble.classList.add('error-bubble');activeRequest=null;showWorking(false)}
async function submit(text){text=String(text||'').trim();if(!text||activeRequest)return;addMessage('user',text);const prompt=$('prompt');if(prompt){prompt.value='';resize()}
  const directMedia=directYoutube(text);
  if(directMedia){const bubble=addMessage('assistant','');bubble.innerHTML=directMedia;const c=$('conversation');if(c)c.scrollTop=c.scrollHeight;return;}
  showWorking(true);const bubble=addMessage('assistant','Sarembok is working…');activeRequest={bubble,finished:false,full:'',onDelta:delta=>{if(!activeRequest||activeRequest.finished)return;activeRequest.full+=String(delta);renderInto(bubble,activeRequest.full,null)}};try{const result=await sendRPC('SarembokChat',{prompt:text,stream:true},activeRequest.onDelta);const finalText=activeRequest.full||resultText(result)||'Directive completed.';finishMessage(finalText,false,result)}catch(error){finishMessage('Execution failed: '+String(error&&error.message||error||'Unknown execution error'),true)}}
function resize(){const p=$('prompt');if(!p)return;p.style.height='auto';p.style.height=Math.min(p.scrollHeight,150)+'px'}
async function refreshRuntime(){try{const r=await sendRPC('GetRuntimeInfo',{});if($('drawer-runtime'))$('drawer-runtime').textContent=r.status||'ONLINE';if($('runtime-detail'))$('runtime-detail').textContent=JSON.stringify(r,null,2);if($('workers-count'))$('workers-count').textContent=String(r.registeredWorkers??r.onlineWorkers??0);if($('agents-count'))$('agents-count').textContent=String(r.activeAgents??0);if($('tasks-count'))$('tasks-count').textContent=String(r.totalTasks??0)}catch(error){if($('drawer-runtime'))$('drawer-runtime').textContent='Unavailable';if($('runtime-detail'))$('runtime-detail').textContent=String(error&&error.message||error||'Runtime query failed');if($('workers-count'))$('workers-count').textContent='Unavailable';if($('agents-count'))$('agents-count').textContent='Unavailable';if($('tasks-count'))$('tasks-count').textContent='Unavailable'}}
function openDrawer(){const d=$('system-drawer');if(!d)return;d.classList.add('open');d.setAttribute('aria-hidden','false');const b=$('drawer-backdrop');if(b)b.hidden=false;refreshRuntime()}
function closeDrawer(){const d=$('system-drawer');if(d){d.classList.remove('open');d.setAttribute('aria-hidden','true')}const b=$('drawer-backdrop');if(b)b.hidden=true}
function stopCurrent(){if(!activeRequest)return;finishMessage('Request stopped by user.',true)}
function boot(){const form=$('chat-form'),prompt=$('prompt');if(form)form.addEventListener('submit',e=>{e.preventDefault();submit(prompt?prompt.value:'')});if(prompt){prompt.addEventListener('input',resize);prompt.addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();submit(prompt.value)}});setTimeout(()=>prompt.focus(),150)}const stop=$('stop-btn');if(stop)stop.addEventListener('click',stopCurrent);const system=$('system-btn');if(system)system.addEventListener('click',openDrawer);const close=$('close-system');if(close)close.addEventListener('click',closeDrawer);const backdrop=$('drawer-backdrop');if(backdrop)backdrop.addEventListener('click',closeDrawer);document.querySelectorAll('[data-prompt]').forEach(button=>button.addEventListener('click',()=>submit(button.dataset.prompt||'')));connect().catch(error=>{setStatus('OFFLINE',false);setMeta('RUNTIME CONNECTION UNAVAILABLE');scheduleReconnect();console.warn('Sarembok connection:',error)})}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();
